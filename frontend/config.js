/*
 * Frontend configuration. This is the ONLY place to change when deploying.
 *
 * Local development:  backend on http://localhost:8080  (default below)
 * Production:         set API_BASE_URL to your deployed backend, e.g.
 *                     "https://trichy-estates-api.onrender.com"
 * (no trailing slash, no "/api" suffix).
 */
window.TRICHY_CONFIG = {
  API_BASE_URL: "http://localhost:8080"
};
